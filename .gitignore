.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

.env
data/*
!data/.gitkeep

# CI 每次自己跑实验并发布, 不需要把产物提交进来。
# 唯一进版本库的实验结果是 README 里那段带时间戳的注入快照。
site/
docs/BENCH.md

# 前端源码在 web/, 但构建产物(keel/server/static)是提交进版本库的 ——
# 这样 clone 下来 pip install 就能起服务, 看项目的人不必先装一套 Node 工具链。
web/node_modules/

.idea/
.vscode/
.DS_Store
面试准备.md

image.png